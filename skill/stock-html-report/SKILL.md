---
name: stock-html-report
description: Generate a single-stock equity research HTML report matching NVDA_Sep_10_2026.html exactly — YK light palette, 940px container, sticky topbar, 7 numbered sections, KPI strip, valuation bar, flag dashboard, bottom-grid and purple hero verdict. Use when user asks for stock report HTML, equity research page, DCF valuation HTML, or "reproduce NVDA report" style.
---

<style>
  /* Color hierarchy — YK palette (light-bg variants) — from NVDA Sep 10 2026 */
  /* Purple = hero (one moment per page). Blue = workhorse structure. Dark = body. */
  h1, h2, h3 { color: #0B5FC0; border-bottom: 1px solid rgba(11,95,192,0.25); padding-bottom: 6px; }
  h1 { color: #692D9A; border-bottom: 2px solid rgba(105,45,154,0.45); }
  .hero { color: #4A1F7A; font-weight: 700; font-size: 1.15em; background: linear-gradient(90deg, rgba(170,77,230,0.08), transparent); padding: 8px 12px; border-left: 4px solid #692D9A; border-radius: 4px; display: block; margin: 12px 0; }
  .accent { color: #0B5FC0; font-weight: 600; }
  .quote { color: #4A1F7A; font-style: italic; border-left: 3px solid #692D9A; padding: 8px 12px; margin: 12px 0; background: rgba(105,45,154,0.05); }
  table { border-collapse: collapse; width: 100%; }
  th { color: #0B5FC0; border-bottom: 2px solid #0B5FC0; padding: 8px; text-align: left; background: rgba(11,95,192,0.05); }
  td { padding: 8px; border-bottom: 1px solid rgba(11,95,192,0.15); }
  hr { border: none; border-top: 1px solid rgba(11,95,192,0.25); margin: 24px 0; }
  strong { color: #1F1B2E; }
  code { color: #692D9A; background: rgba(105,45,154,0.08); padding: 1px 6px; border-radius: 4px; }
</style>

# stock-html-report — Skill Specification

> **Source of truth (only file):** `C:\Users\Yogesh Kumar\Downloads\CLAUDE-CODE\Stock\2026-09\NVDA_Sep_10_2026.html` — 565 lines, read 2026-09-12. This skill codifies *exactly* what that file does so any agent can reproduce it byte-for-byte without guessing. No other file was consulted.
>
> **Scope:** One self-contained equity-research HTML page. The output is **one `.html` file** that is ready to open in a browser — no external CSS/JS, one `<style>` block, one `<script>` block for smooth-scroll. Same 7 sections, same YK light palette, same purple hero verdict.
>
> **Invocation:** `stock-html-report` or natural language — *"make an NVDA-style HTML report"* / *"equity research HTML like NVDA Sep 10"* / *"generate stock HTML report"*.

---

## What the NVDA file actually is (audit 2026-09-12)

| Fact | Value |
|---|---|
| File | `NVDA_Sep_10_2026.html` — 565 lines, `<!DOCTYPE html>` |
| Title | `NVDA — Equity Research · September 10, 2026` |
| Design contract | `<style>` comment on line 8: `stock-html-report · demo-report.css — Verbatim copy of the <style> block from stock-ui/demo.html (IRCTC, Sep 3 2026)` |
| Palette | YK light-bg — **not** the red `#B91C1C` MD palette. Numbers are **purple** `code { color: #692D9A }` on `rgba(105,45,154,0.08)` |
| Container | `max-width: 940px`, page `linear-gradient(180deg, #f8f9fb → #f1f3f8)`, surface `#ffffff` |
| Sections | Exactly **7 numbered `<h2>`** with `<span class="num">` purple number prefix |
| Footer signature | `Generated by stock-html-report skill · demo.html light palette` |

---

## When to use / when not to

**Use for:**
- Single-name equity research HTML page in the NVDA layout (NSE/BSE/NASDAQ — adapts currency).
- User pastes financials and wants a browser-ready report, or says "like NVDA Sep 10 HTML".
- Any request for a styled valuation page with a valuation bar, flag dashboard, and hero verdict.

**Not for:**
- Markdown-only reports (that is `stock-analysis` / `text-summary`).
- Watchlists, screeners, or multi-ticker comparisons — one ticker = one HTML file.

---

## The output shape — one HTML page, 7 sections in order

Headers are the numbered `<h2>` exactly as in NVDA. No section may be reordered or skipped unless source genuinely has no data.

```text
Topbar (sticky) + page-head (kicker + H1 + meta-grid + valuation-status) + kpi-row (4 cards)
  ↓
1. COMPANY OVERVIEW       intro <p> + muted two-platforms <p> + 3-col platform table + .quote Strategic Position + <hr>
2. KEY COMPETITIVE STRENGTHS  muted intro <p> + 2-col moat table (emoji in Strength cell, code numbers in Details) + <hr>
3. FINANCIAL SNAPSHOT & QUALITY READ  muted intro <p> + 4-col table (Metric/TTM/% MCap/Read) + .quote.warning BALANCE-SHEET WARNING + <hr>
4. VALUATION MULTIPLES    muted intro <p> + 4-col table (Metric/Value/Benchmark/Read 🟢🟡🔴) + footnote <p> + .quote benchmarks are approximate + <hr>
5. DCF & INTRINSIC VALUATION  3-col valuation table + .quote 📌 METHODOLOGY NOTE + .val-visual Valuation Bar + <ul> metadata + .card ⚙️ DCF Parameters + <hr>
6. RED FLAGS              <ul> intro + 4-col numbered table (#/Flag/Severity/Evidence) + .dashboard flag-grid + <h3> + <pre><code> ASCII strip + <hr>
7. BOTTOM LINE & VERDICT  .quote RESEARCH SUMMARY (2 <p>) + .quote ✅❌ + centered italic teaser <p> + .bottom-grid (6 cards) + .hero 🎯 verdict + <footer>
```

---

## Design contract — the `<style>` block (copy verbatim, do not invent tokens)

The NVDA file's `<style>` is **256 lines** (lines 8–255). It is the source of truth — match it exactly. Key tokens:

```css
:root {
  --purple: #692D9A; --purple-text: #4A1F7A; --purple-soft: rgba(105,45,154,0.08);
  --purple-bdr: rgba(105,45,154,0.45); --purple-glow: rgba(170,77,230,0.08);
  --blue: #0B5FC0; --blue-soft: rgba(11,95,192,0.05); --blue-bdr: rgba(11,95,192,0.15);
  --blue-bdr2: rgba(11,95,192,0.25);
  --bg: #f8f9fb; --bg2: #f1f3f8; --surface: #ffffff; --surface-2: #f1f5f9; --border: #e2e8f0;
  --text: #1F1B2E; --muted: #64748b; --muted2: #94a3b8;
  --gold: #92400e; --gold-bg: #fef3c7; --gold-bdr: #fde68a;
  --red: #dc2626; --red-soft: #fef2f2; --green: #059669; --amber: #d97706;
}
```

Critical rules derived from NVDA (do not change):
- `h1 { color: var(--purple); font-size: 2.2rem; font-weight: 900; border-bottom: 2px solid var(--purple-bdr); display: inline-block }`
- `h2 { color: var(--blue); font-size: 1.32rem; font-weight: 800; border-bottom: 1px solid var(--blue-bdr2) }` + `h2 .num { color: var(--purple) }`
- `code { color: var(--purple); background: var(--purple-soft); border: 1px solid rgba(105,45,154,0.12) }` — **purple numbers, not red** — this file uses purple codes
- `code.gold { color: var(--gold); background: var(--gold-bg) }` — reserved for future gold variant
- `.hero { border-left: 4px solid var(--purple); background: linear-gradient(90deg, rgba(170,77,230,0.08), transparent) }` — single hero moment
- `.quote.warning { border-left-color: var(--red); background: var(--red-soft); color: #7f1d1d }` — only warning quote uses red
- Cards: `.card`, `.meta-card`, `.kpi`, `.bottom-card`, `.val-visual`, `.dashboard` all `background: var(--surface); border: 1px solid var(--border); border-radius: 12-14px; box-shadow: 0 1px 3px rgba(15,23,42,0.04)`
- Pills: `.pill.green/.amber/.red/.blue/.purple` — status pills in meta-grid and table
- Responsive: `.meta-grid` 3→2→1 cols at 680/420px, `.kpi-row` 4→2→1 at 720/420, `.bottom-grid` 2→1 at 680, `.flag-grid` 5→3→2 at 640/420

> Full 256-line block is in the source file lines 8–255. When generating, copy it verbatim — line breaks, comments, and the `/* stock-html-report · demo-report.css */` header comment are part of the contract.

---

## File shell (mandatory structure)

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>{TICKER} — Equity Research · {Month D, YYYY}</title>
<base target="_self">
<style>/* stock-html-report · demo-report.css — verbatim 256-line block */</style>
</head>
<body>
<div class="topbar">…</div>
<div class="container">
  <header class="page-head">…</header>
  <div class="kpi-row">…</div>
  <h2 id="overview"><span class="num">1.</span> COMPANY OVERVIEW</h2> …
  <h2 id="strengths"><span class="num">2.</span> KEY COMPETITIVE STRENGTHS</h2> …
  <h2 id="financials"><span class="num">3.</span> FINANCIAL SNAPSHOT & QUALITY READ</h2> …
  <h2 id="valuation"><span class="num">4.</span> VALUATION MULTIPLES</h2> …
  <h2><span class="num">5.</span> DCF & INTRINSIC VALUATION</h2> …
  <h2 id="flags"><span class="num">6.</span> RED FLAGS — {TICKER}</h2> …
  <h2 id="verdict"><span class="num">7.</span> BOTTOM LINE & INVESTMENT VERDICT</h2> …
  <footer>…</footer>
</div>
<script>/* smooth-scroll 70px offset — verbatim from NVDA lines 539-563 */</script>
</body>
</html>
```

---

## Section-by-section rules (codified from NVDA only)

### Topbar (sticky, `z-index:40`, `backdrop-filter: blur(10px)`)
- `.topbar-inner` max-width 940px, flex space-between
- Left `.brand`: `.brand-mark` 36px gradient `linear-gradient(135deg,#692D9A,#0B5FC0)` with emoji (NVDA uses `📊`), `<b>TICKER</b><span>· EXCHANGE · Equity Research</span>`, plus `.pill.blue` live price (`Live $223.67`)
- Right `.nav`: 6 pills — `Overview`, `Moat`, `Financials`, `Valuation`, `Red Flags`, `Verdict` — each `href="#{id}"` with blue-soft pill style

### Header `.page-head` (centered)
- `.kicker` blue pill: `Stock · Equity Research · {TICKER} Report · {Month Day}` — uppercase 0.72rem, letter-spacing 0.16em
- `h1`: `EQUITY RESEARCH REPORT: {COMPANY}` + `<span class="sub">` sector line (NVDA: `Semiconductors — AI Accelerated Computing & Data Center Infrastructure (Mega-cap)`)
- `.meta-grid` 6 cards: Ticker (`<code>NASDAQ: NVDA</code>`), Report Date, Live Price (`<code>$223.67</code>` + muted date), Market Cap (`<code>$5.44T</code>`), Sector, Valuation Status (`<span class="pill amber">🟡 FAIRLY VALUED…</span>`)
- `.valuation-status` purple left-border callout: one-line caveat with backticked numbers + `.btn-row` with `a.btn.primary` → `#valuation` and `a.btn` → `#flags`

### KPI strip `.kpi-row` (4 cards)
- Each `.kpi`: `.lbl` uppercase 0.68rem, `.val` 1.25rem 900, `.sub` 0.78rem muted with backticked yields/multiples
- NVDA values: Revenue `$303.00B` (`5.57%` MCap, `17.95x` P/S) · Net Profit `$192.88B` (`28.3x` P/E, EPS `$7.91`) · FCF `$127.01B` (`2.33%` yield, `42.8x` P/FCF) · Margins `75.0%` GM / `65.2%` OPM / Net Cash `+$40.36B` / D/E `0.07`

### 1. Company Overview (`#overview`)
- Intro `<p>`: bold company name + bold positioning line (NVDA: *default infrastructure layer of the AI factory*), include market cap + price + shares
- Muted `<p style="font-size:0.88rem;color:var(--muted)">`: two-platform split with backticked percentages and GM
- Table 3 cols: `Platform | Q1 FY27 (centered) | Share (centered)` — rows for Data Center (with indented ↳ Compute/Networking), Edge Computing, Total, TTM
- Closing `.quote`: **Strategic Position:** one line with backticked TTM/margin/multiple figures

### 2. Key Competitive Strengths (`#strengths`)
- Muted intro `<p>`: "Four tests: …" with backticked numbers
- Table 2 cols: `Strength | Details` — 5 rows, emoji in bold Strength cell (`🧠 🏭 🔗 💰 🌍`), every Details cell carries at least one backticked number

### 3. Financial Snapshot & Quality Read (`#financials`)
- Muted intro `<p>`: TTM window + quality-vs-cash framing
- Table 4 cols: `Metric | TTM (center) | % MCap (center) | Read` — 5 rows: Market Cap, Revenue, Net Profit, FCF, Net Cash — each with backticked numbers and yield/multiple reads
- `.quote.warning`: **⚠️ BALANCE-SHEET WARNING:** — covers WC absorption, receivables/inventories/commitments, GAAP vs non-GAAP gap, liquidity vs debt, and the one-line condition ("if Blackwell yields hold and `$108B` Q3 converts")

### 4. Valuation Multiples (`#valuation`)
- Muted intro `<p>`: "Read as a system — market prices **durability**, not failure"
- Table 4 cols: `Metric | Value (center) | Benchmark (center) | Read` — 6 rows: Trailing P/E, Forward P/E, EV/EBITDA, P/Book, P/S, PEG — Read column uses 🟡🟢🔴 with backticked discount/premium
- Footnote `* Benchmarks approximate…` in 0.76rem muted `<p>`, then `.quote` repeating "Historical and sector reference multiples are **approximate**…"

### 5. DCF & Intrinsic Valuation (no id on NVDA — keep consistent)
- Table 3 cols: `Approach | IV / Share (center) | vs $price` — 4 rows: DCF Base (WACC `9.50%`, g `4.50%`) `$211` 🟡, Peer `25x × EBITDA $240B` `$242` 🟢, Blended `$227` 🟡, Buy Zone (`20%` MOS) `$181` 🎯
- `.quote` 📌 **METHODOLOGY NOTE:** — blended equally weights DCF cash-truth + peer earnings-power, state base FCF `$127.01B` with `25%→5%` fade 10Y + terminal `4.50%`, peer `25x` on EBITDA `$240B`, "Neither is a vendor quote"
- `.val-visual`: title `📈 Valuation Bar`, mono flex labels `LOW $180 · CURRENT $223.67 · BUY $181 · IV $227 · DCF $211 · PEER $242`, `.bar-track` with `.bar-fill` at `width:72%` (gradient `90deg,#0B5FC0,#692D9A`), `.bar-marks` `$180 | IV $227 | $242`, explainer line with backticked numbers, WS avg line `WS avg $323.42 +44.6% (61 analysts, Strong Buy) | Low $180 | Med $315 | High $515 | Shares 24.39B | DCF Sep 10, 2026`, `.legend` Low / Current→Buy Zone / Intrinsic Value
- `<ul>` 5 bullets: Blended IV, Buy Zone, DCF Base + Peer, Shares, Dates
- `.card` ⚙️ DCF Parameters: 4 `<li>` — WACC `9.50%` (Rf `4.25%`, ERP `5.50%`, Beta `1.65`, CoE `13.33%`), Base FCF `$127.01B` + forecast fade `25%→5%`, Terminal `4.50%` + Net Cash `+$40.36B` + Assets `$259.47B`, Shares `24.39B` (buybacks `$80B` authorized)

### 6. Red Flags (`#flags`)
- `<ul>` 3 bullets: thesis risk, China regime, Severity legend with `.pill.red` 🔴 High + `.pill.amber` 🟡 Medium
- Table 4 cols: `# | Red Flag | Severity | Evidence` — 5 rows, Severity uses `.pill.red` 🔴 High (first 2) + `.pill.amber` 🟡 Med (last 3), Evidence cells backticked
- `.dashboard` with `.flag-grid` 5 flags (HYPER SCL CONCENTR, CHINA OVERHANG, SUPPLY INTENSITY, EQUITY QUALITY, MULTIPLE RISK) each `<b>` + `<span class="sev high/med">`, plus centered count `2 HIGH · 3 MEDIUM · 0 LOW`
- `<h3>Red Flag Dashboard</h3>` + `<pre><code>` ASCII strip 3-line (labels + emoji + severity) — same data as dashboard, monospaced 0.78rem

### 7. Bottom Line & Verdict (`#verdict`)
- `.quote` RESEARCH SUMMARY: 2 `<p>` — first with yields/market cap, second with positioning metaphor ("AI factory builder… single product") + blended DCF fair value + `$119B` commitments / `~50%` hyperscaler concentration caveat
- `.quote` second block: 3 `<p>` — ✅ **Strengths:** (5 bullets inline with `·`), ❌ **Concerns:** (inline), **The blended DCF…** + **Disciplined Margin-of-Safety Buy Zone: `$181`** (`23.5%` above entry / *wait for a scare*)
- Centered muted italic `<p>`: `Bottom Line — in plain language, no spreadsheet needed.`
- `.bottom-grid` 6 cards (2 cols → 1 col at 680px):
  1. `🧠 The Core Idea` — factory builder, not chip seller
  2. `🔄 What Changed` — graphics cards → clusters
  3. `👀 Hidden Insight` — networking fabric is the lock-in
  4. `⚠️ What Could Break It` — concentration + supply intensity
  5. `❓ The Open Question` — can hyperscalers sustain pace + can NVIDIA supply?
  6. `🎯 Takeaway` `.takeaway` purple-left-border — *Don't pay for perfection…*
- `.hero` with `.label` 🎯 The Verdict — hero takeaway — one question covering `$108B` next quarter at `75%` GM, converting `$119B` commitments + `$40.71B` receivables, compounding at `$227` vs disciplined `$181`
- `<footer>`: italic disclaimer (educational-only, sources: `NVIDIA SEC filings (Q2 FY27 Form 10-Q filed Aug 26, 2026; FY2026 Form 10-K filed Feb 26, 2026)`, Nasdaq market data Sep 9 2026 close, sell-side consensus `S&P Global, MarketBeat` as of Sep 10 2026, DCF independently constructed not vendor, benchmarks approximate, consensus vs guidance `$0` China, consult registered advisor) + bold generation line `NVDA · NASDAQ: NVDA · September 10, 2026 · Generated by stock-html-report skill · demo.html light palette` + muted live-stats line

### Script (verbatim, bottom of `<body>`)
- Smooth-scroll handler: `document.addEventListener('click', …)` intercepts `href^="#"`, prevents default, scrolls to `getBoundingClientRect().top + pageYOffset - 70` with `behavior: 'smooth'`; on `location.hash` on load, scrolls without smooth. Keep `70px` offset to clear sticky topbar. Copy lines 539–563 verbatim.

---

## Color hierarchy — YK light-bg (from NVDA, verbatim)

| Color | Hex | Use | Max |
|---|---|---|---|
| **Purple (deep)** | `#692D9A` | H1 + `.bar-fill` gradient end | 1 |
| **Purple (hero text)** | `#4A1F7A` | `.hero` + `.quote` + `.val-visual` explainer accent | 1 hero |
| **Purple (soft)** | `rgba(105,45,154,0.08)` | `code` bg + `.hero` gradient | many (numbers) |
| **Blue (workhorse)** | `#0B5FC0` | H2/H3, `th`, pills, links, `.bar-fill` gradient start | many |
| **Dark body** | `#1F1B2E` | Paragraphs, lists, `.kpi .val` | bulk |
| **Muted** | `#64748b` | Sub-lines, muted intros, marks | bulk |
| **Red** | `#dc2626` on `#fef2f2` | `.quote.warning` + `.pill.red` + `.sev.high` | warnings only |
| **Amber/Gold** | `#d97706` / `#92400e` | `.pill.amber` + `code.gold` | status only |
| **Green** | `#059669` | `.pill.green` | status only |

**Rule:** Purple earns its place once — on the hero verdict. Blue holds structure. Red/amber only for warnings/pills. Bulk is dark/muted. Code numbers are purple-on-lavender (not red — that belongs to the MD skill).

---

## File naming and output

**Primary artifact:** One self-contained `.html`.

```text
Stock/<YYYY-MM>/{TICKER}_Sep_{D}_{YYYY}.html
# NVDA example: Stock/2026-09/NVDA_Sep_10_2026.html

# Alternative if user requests parallel MD-style folder:
{TICKER}_Sep_{D}_{YYYY}.html  (same basename as MD, but HTML-only)
```

- On-disk NVDA uses abbrev `Sep` (not `September`). Write `Sep` to stay compatible.
- One `.html` per ticker-date. Do not write two HTML files unless user explicitly asks for a theme variant.
- If a variant is requested, append theme suffix: `_Blue.html`, `_Purple.html`, `_Dark.html` — but the NVDA default (light-bg purple/blue) is the canonical form.

---

## Procedure when invoked

1. **Confirm ticker + report date + exchange + currency.** If user gives ticker without date, ask — do not guess. Map `NSE/BSE → ₹/Cr`, `NASDAQ/NYSE → $/B/T`, `BSE code` dual-list if provided.
2. **Gather financials** (live price, market cap, TTM revenue/NP/FCF, margins, D/E, cash, segment splits, consensus target + 52W range + analyst count + recent move). Cite sources in footer disclaimer; note vendor blocks (e.g., Alpha Spread 403) explicitly in methodology note if encountered.
3. **Run DCF independently** if no vendor DCF. Disclose every parameter in `⚙️ DCF Parameters` (WACC, Rf, ERP, Beta, CoE, base FCF, growth fade `25%→5%` over 10Y, terminal g, net cash, assets, shares, buyback auth). State "independently modelled, not vendor-quoted" in 📌 note. Choose blended weighting: NVDA equally weights `DCF $211` + `Peer 25x EBITDA $240B → $242` → `Blended $227`; adapt multiples to ticker but keep 4-row shape.
4. **Assemble HTML in order:** `<!DOCTYPE>` → `<style>` verbatim (256 lines) → `.topbar` → `.container` → `.page-head` → `.kpi-row` → `1→7` sections with `<hr>` between → `<footer>` → `<script>` verbatim. Numbers in `<code>` (purple), status in `.pill`, hero in `<span class="hero">`-style `.hero`.
5. **Validate before success:** `sections == 7` in order, `.topbar` + `.meta-grid` (6 cards) + `.kpi-row` (4 cards) + `.val-visual` + `.dashboard` + `.bottom-grid` (6 cards) + exactly one `.hero` + exactly two `.quote` in §7 (research summary + strengths/concerns) + one `.quote.warning` in §3, `<style>` contains `--purple: #692D9A` and `code { color: var(--purple) }`, `<script>` scroll offset `70`, `lines > 400`, file opens as valid HTML.
6. **Print 3–5 line preview + file path.** Do not generate a second theme pre-emptively.

---

## Quality gate (run before saving)

```text
SHELL   ☐ <!DOCTYPE html> + <meta viewport> + <base target="_self"> + 256-line <style> verbatim + smooth-scroll <script> (70px offset)
HEADER  ☐ .topbar sticky with brand-mark gradient + 6 nav pills + .page-head kicker + h1 + sub + 6-card meta-grid + valuation-status callout + 2 btns
KPI     ☐ .kpi-row 4 cards (Revenue/net profit/FCF/margins) each with backticked yields/multiples
STRUCT  ☐ Exactly 7 numbered <h2> in order (1 OVERVIEW … 7 VERDICT) + <hr> between + 940px container
TABLES  ☐ Every number in <code> = purple #692D9A ☐ §1 platform table 3-col ☐ §3 4-col ☐ §4 4-col with 🟢🟡🔴 ☐ §5 4-row DCF + 📌 note ☐ §6 5 flags + dashboard + <pre> strip ☐ No bullets inside tables
VISUALS ☐ .val-visual with bar-track + bar-fill width% + bar-marks + legend ☐ .dashboard flag-grid 5 cols ☐ .bottom-grid 6 cards (last is .takeaway purple)
HERO    ☐ Exactly one .hero purple-left-border verdict (🎯 + single question) ☐ Exactly two .quote in §7 + one .quote.warning in §3
FOOTER  ☐ Italic disclaimer (SEC/Nasdaq/consensus + DCF not vendor + benchmarks approximate + consult advisor) + generation line + live-stats line
DELIVERY☐ File at Stock/<YYYY-MM>/{TICKER}_Sep_{D}_{YYYY}.html (abbrev Sep) ☐ One HTML only ☐ Valid HTML, lines > 400
```

> **If 80% of prose were removed, would the purple numbers, tables, bar, and dashboard still carry the argument?** If not, restructure.

---

## What this skill is not

- **Not `stock-analysis`.** That builds a styled `.md` with red `#B91C1C` numbers, ALL-CAPS Markdown headings, and a Markdown valuation bar. This builds a browser-ready `.html` with purple `#692D9A` numbers, sticky topbar, and CSS bar/dashboard.
- **Not financial advice.** The italic disclaimer at the bottom of every report is mandatory.
- **Not a screener.** One ticker = one HTML report.

---

## House style notes (from NVDA)

- **Two decimals where source provides them** — ` $223.67`, `$5.44T`, `75.0%` — don't round.
- **Backtick every quotable number** via `<code>` — percentages, multiples, dollar amounts, share counts. Don't code years or dates.
- **One emoji per row max** in tables. 🟢 🔴 🟡 🎯 📌 ⚠️ ✅ ❌ only. Brand mark uses `📊` (adapt per ticker if user requests, but `📊` is canonical).
- **Blockquotes for meta-commentary** — `.quote` (lavender left bar) and `.quote.warning` (red) — they render with a left border.
- **US note:** For NASDAQ/NYSE use `$` + `B`/`T`, keep code purple, keep `% of Market Cap` column. For NSE/BSE use `₹` + `Cr`/`Lakh Cr` with same layout.
- **Attribution:** Footer must name `NVIDIA SEC filings (Q2 FY27 Form 10-Q filed Aug 26, 2026; FY2026 Form 10-K filed Feb 26, 2026)` pattern for the covered ticker (swap filing names/dates per company), plus Nasdaq/NSE market data date and consensus sources.

---

*Last audited: 2026-09-12 against `C:\Users\Yogesh Kumar\Downloads\CLAUDE-CODE\Stock\2026-09\NVDA_Sep_10_2026.html` — 565 lines, 7 sections, YK light-bg verbatim, purple code numbers, 256-line style block, twin-ship via demo-report.css, topbar + kpi-row + val-visual + flag-grid + bottom-grid + hero + 70px scroll script. Spec version: v1-nvda-audit.*
